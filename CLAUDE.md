@AGENTS.md

## Claude Code Notes

- Claude-specific packaging lives in `.claude-plugin/`.
- The wrapper under `skills/deeppapernote/` should stay thin and point back to the root `SKILL.md`.
- Do not fork or restate the DeepPaperNote workflow in Claude-only wrappers.
